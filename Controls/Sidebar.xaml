<UserControl x:Class="CarRental.Desktop.Controls.Sidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="250">
    <Border Background="{StaticResource GlassSurfaceBrush}" BorderBrush="{StaticResource NeonBorderBrush}" BorderThickness="0,0,1,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo / Brand -->
            <StackPanel Grid.Row="0" Margin="20,30,20,40">
                <TextBlock Text="CAR RENTAL" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="Back-office" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Navigation Menu -->
            <StackPanel Grid.Row="1" Margin="10,0">
                <Button Command="{Binding NavigateToDashboardCommand}" Content="Dashboard" Margin="0,0,0,10" HorizontalContentAlignment="Left"/>
                <Button Command="{Binding NavigateToVehiclesCommand}" Content="Vehicles" Margin="0,0,0,10" HorizontalContentAlignment="Left"/>
                <Button Command="{Binding NavigateToVehicleTypesCommand}" Content="Vehicle Types" Margin="0,0,0,10" HorizontalContentAlignment="Left"/>
                <Button Command="{Binding NavigateToRentalsCommand}" Content="Rentals" Margin="0,0,0,10" HorizontalContentAlignment="Left"/>
                <Button Command="{Binding NavigateToClientsCommand}" Content="Clients" Margin="0,0,0,10" HorizontalContentAlignment="Left"/>
                <Button Command="{Binding NavigateToEmployeesCommand}" Content="Employees" Margin="0,0,0,10" HorizontalContentAlignment="Left"/>
                <Button Command="{Binding NavigateToAvailabilityCommand}" Content="Availability" Margin="0,0,0,10" HorizontalContentAlignment="Left"/>
                <Button Command="{Binding NavigateToPaymentsCommand}" Content="Payments" Margin="0,0,0,10" HorizontalContentAlignment="Left"/>
                <Button Command="{Binding NavigateToAlertsCommand}" Content="Alerts" Margin="0,0,0,10" HorizontalContentAlignment="Left"/>
            </StackPanel>

            <!-- Logout / User Info -->
            <StackPanel Grid.Row="2" Margin="20,30">
                <Button Command="{Binding NavigateToLoginCommand}" Content="Logout" Background="Transparent" BorderBrush="{StaticResource ErrorBrush}" BorderThickness="1" Foreground="{StaticResource ErrorBrush}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
