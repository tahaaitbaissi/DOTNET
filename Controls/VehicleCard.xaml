<UserControl x:Class="CarRental.Desktop.Controls.VehicleCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="200">
    <Border Background="{StaticResource GlassSurfaceBrush}" BorderBrush="{StaticResource NeonBorderBrush}" BorderThickness="1" CornerRadius="10" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="5" ShadowDepth="2"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="140"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Image Placeholder or Image -->
            <Border Grid.Row="0" CornerRadius="10,10,0,0" Background="#F0F0F0" ClipToBounds="True">
                <Image Source="{Binding ImagePath}" Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <!-- Info -->
            <StackPanel Grid.Row="1" Margin="10">
                <TextBlock Text="{Binding Make}" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                <TextBlock Text="{Binding Model}" FontWeight="Bold" Foreground="{StaticResource TextBrush}" FontSize="16"/>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <TextBlock Text="{Binding Year}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,10,0"/>
                    <Border Background="{StaticResource PrimaryBrush}" CornerRadius="3" Padding="5,1">
                        <TextBlock Text="{Binding Status}" Foreground="White" FontSize="10"/>
                    </Border>
                </StackPanel>
                <TextBlock Text="{Binding DailyRate, StringFormat={}{0:C}/day}" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Right" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
