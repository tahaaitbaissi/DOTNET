<UserControl x:Class="CarRental.Desktop.Controls.Header"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="800">
    <Border Background="{StaticResource GlassSurfaceBrush}" BorderBrush="{StaticResource NeonBorderBrush}" BorderThickness="0,0,0,1" Padding="20,0">
        <Grid>
            <!-- Bind Title to the DataContext (MainViewModel).CurrentView.Title -->
            <TextBlock Text="{Binding CurrentView.Title, TargetNullValue=Dashboard, FallbackValue=Dashboard}" 
                       VerticalAlignment="Center" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="Admin User" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextSecondaryBrush}"/>
                <Ellipse Width="35" Height="35" Fill="{StaticResource PrimaryBrush}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
