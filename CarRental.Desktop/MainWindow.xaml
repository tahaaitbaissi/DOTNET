<Window x:Class="CarRental.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:CarRental.Desktop.ViewModels"
        xmlns:views="clr-namespace:CarRental.Desktop.Views"
        xmlns:controls="clr-namespace:CarRental.Desktop.Controls"
        mc:Ignorable="d"
        Title="Car Rental Back-office" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">

    <Window.Background>
        <RadialGradientBrush Center="0.5,0.0" GradientOrigin="0.5,0.0" RadiusX="1.0" RadiusY="1.0">
            <GradientStop Color="#2A2D3E" Offset="0.0"/>
            <GradientStop Color="#1E1E2F" Offset="1.0"/>
            <GradientStop Color="#0F0F1A" Offset="1.5"/>
        </RadialGradientBrush>
    </Window.Background>

    <Window.Resources>
        <!-- Basic ViewModels -->
        <DataTemplate DataType="{x:Type viewmodels:DashboardViewModel}">
            <views:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:VehiclesViewModel}">
            <views:VehiclesView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:RentalsViewModel}">
            <views:RentalsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ClientsViewModel}">
            <views:ClientsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:EmployeesViewModel}">
            <views:EmployeesView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:AvailabilityViewModel}">
            <views:AvailabilityView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:PaymentsViewModel}">
            <views:PaymentsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:AlertsViewModel}">
            <views:AlertsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:LoginViewModel}">
            <views:LoginView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:VehicleTypesViewModel}">
            <views:VehicleTypesView/>
        </DataTemplate>
        
        <!-- Management ViewModels (used by navigation) -->
        <DataTemplate DataType="{x:Type viewmodels:VehicleManagementViewModel}">
            <views:VehicleManagementView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ClientManagementViewModel}">
            <views:ClientManagementView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:BookingManagementViewModel}">
            <views:BookingManagementView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:PaymentManagementViewModel}">
            <views:PaymentManagementView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:MaintenanceViewModel}">
            <views:MaintenanceView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ReportViewModel}">
            <views:ReportView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <controls:Sidebar Grid.Column="0" DataContext="{Binding}"/>

        <!-- Main Content Area -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <controls:Header Grid.Row="0" DataContext="{Binding}"/>

            <!-- Content -->
            <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" Margin="20"/>
        </Grid>
    </Grid>
</Window>
