<UserControl x:Class="CarRental.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:CarRental.Desktop.Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Border Background="{StaticResource SurfaceBrush}" Width="400" Height="450" CornerRadius="15" Padding="40">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Welcome Back" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextBrush}" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Sign in to continue" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,40"/>

                <TextBlock Text="Email" Style="{StaticResource {x:Type TextBlock}}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="40" Margin="0,0,0,20"/>

                <TextBlock Text="Password" Style="{StaticResource {x:Type TextBlock}}" Margin="0,0,0,5"/>
                <PasswordBox controls:PasswordBoxHelper.Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             controls:PasswordBoxHelper.Attach="True"
                             Height="40" Margin="0,0,0,30"/>
                
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" Margin="0,0,0,10" TextWrapping="Wrap" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

                <Button Content="Login" Height="45" Command="{Binding LoginCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
