<UserControl x:Class="CarRental.Desktop.Views.BookingManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Gestion des Réservations" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    Margin="0,0,0,10">
            <Button Content="🔄 Actualiser" 
                    Command="{Binding RefreshCommand}" 
                    Width="120" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Style="{StaticResource ModernButtonStyle}"/>
            <Button Content="✅ Confirmer" 
                    Command="{Binding ConfirmBookingCommand}" 
                    Width="120" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Background="#28a745"
                    Style="{StaticResource ModernButtonStyle}"/>
            <Button Content="✔️ Compléter" 
                    Command="{Binding CompleteBookingCommand}" 
                    Width="120" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Background="#007bff"
                    Style="{StaticResource ModernButtonStyle}"/>
            <Button Content="❌ Annuler" 
                    Command="{Binding CancelBookingCommand}" 
                    Width="120" 
                    Height="35" 
                    Margin="0,0,10,0"
                    Background="#dc3545"
                    Style="{StaticResource ModernButtonStyle}"/>
            <Button Content="🖨️ Imprimer" 
                    Command="{Binding PrintInvoiceCommand}" 
                    Width="120" 
                    Height="35"
                    Style="{StaticResource ModernButtonStyle}"/>
        </StackPanel>

        <!-- Bookings DataGrid -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Bookings}" 
                  SelectedItem="{Binding SelectedBooking}"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="#F5F5F5"
                  RowHeight="40">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Client" Binding="{Binding ClientName}" Width="150"/>
                <DataGridTextColumn Header="Véhicule" Binding="{Binding VehicleName}" Width="200"/>
                <DataGridTextColumn Header="Début" Binding="{Binding StartDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                <DataGridTextColumn Header="Fin" Binding="{Binding EndDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                <DataGridTextColumn Header="Montant" Binding="{Binding TotalAmount, StringFormat=C}" Width="100"/>
                <DataGridTemplateColumn Header="Statut" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="3" Padding="5,2">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                <Setter Property="Background" Value="#ffc107"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Confirmed">
                                                <Setter Property="Background" Value="#28a745"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                <Setter Property="Background" Value="#6c757d"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                                <Setter Property="Background" Value="#dc3545"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Status}" 
                                          Foreground="White" 
                                          FontWeight="Bold"
                                          HorizontalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Payé" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding IsPaid, Converter={StaticResource BoolToYesNoConverter}}" 
                                      HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <Border Grid.Row="3" 
                Background="#f8f9fa" 
                BorderBrush="#dee2e6" 
                BorderThickness="0,1,0,0" 
                Padding="10" 
                Margin="0,10,0,0">
            <TextBlock>
                <Run Text="Total: "/>
                <Run Text="{Binding Bookings.Count, Mode=OneWay}" FontWeight="Bold"/>
                <Run Text=" réservations"/>
            </TextBlock>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="4" 
                Background="#80000000" 
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" 
                       VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" 
                            Width="200" 
                            Height="20"/>
                <TextBlock Text="Chargement..." 
                          Foreground="White" 
                          Margin="0,10,0,0" 
                          FontSize="16"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>