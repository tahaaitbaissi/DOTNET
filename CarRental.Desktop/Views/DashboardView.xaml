<UserControl x:Class="CarRental.Desktop.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:CarRental.Desktop.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- KPI Cards -->
        <UniformGrid Grid.Row="0" Columns="3" Margin="0,0,0,20">
            <!-- Total Vehicles -->
            <controls:InfoCard Margin="0,0,10,0"
                               Title="Total Vehicles" 
                               Value="{Binding TotalVehicles}" 
                               Icon="🚗"/>

            <!-- Active Rentals -->
            <controls:InfoCard Margin="10,0"
                               Title="Active Rentals" 
                               Value="{Binding ActiveRentals}" 
                               Icon="🔑"/>

            <!-- Total Revenue -->
            <controls:InfoCard Margin="10,0,0,0"
                               Title="Total Revenue" 
                               Value="{Binding TotalRevenue, StringFormat={}{0:C}}" 
                               Icon="💰"/>
        </UniformGrid>

        <!-- Main Content (Charts & Activity) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Chart Placeholder -->
            <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" CornerRadius="10" Padding="20" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Revenue Overview" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                    <Border Grid.Row="1" BorderBrush="{StaticResource TextSecondaryBrush}" BorderThickness="1" CornerRadius="5" Margin="0,20,0,0" Opacity="0.3">
                        <TextBlock Text="Chart Area (Placeholder)" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </Border>
                </Grid>
            </Border>

            <!-- Recent Activity -->
            <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" CornerRadius="10" Padding="20" Margin="10,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Recent Activity" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                    
                    <ItemsControl Grid.Row="1" ItemsSource="{Binding RecentActivity}" Margin="0,20,0,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{StaticResource BackgroundBrush}" BorderThickness="0,0,0,1" Padding="0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Ellipse Width="10" Height="10" Fill="{StaticResource PrimaryBrush}" VerticalAlignment="Top" Margin="0,5,10,0"/>
                                        <TextBlock Grid.Column="1" Text="{Binding}" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
