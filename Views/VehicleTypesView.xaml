<UserControl x:Class="CarRental.Desktop.Views.VehicleTypesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="+ Add Type" Command="{Binding AddCommand}" Width="150"/>
            <Button Content="Edit" Command="{Binding EditCommand}" Margin="10,0,0,0" Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource TextBrush}"/>
            <Button Content="Delete" Command="{Binding DeleteCommand}" Margin="10,0,0,0" Background="{StaticResource ErrorBrush}" Foreground="White"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding VehicleTypes}" SelectedItem="{Binding SelectedVehicleType}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Base Price" Binding="{Binding BasePrice, StringFormat=C}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Edit Overlay -->
        <Grid Grid.RowSpan="2" Background="#AA000000" Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="{StaticResource BackgroundBrush}" Width="400" Height="450" CornerRadius="10" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="30">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="{Binding EditTitle}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,20"/>

                    <ScrollViewer Grid.Row="1">
                        <StackPanel>
                            <Label Content="Name"/>
                            <TextBox Text="{Binding Name}" Margin="0,0,0,15"/>

                            <Label Content="Description"/>
                            <TextBox Text="{Binding Description}" Margin="0,0,0,15" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>

                            <Label Content="Base Price"/>
                            <TextBox Text="{Binding BasePrice}" Margin="0,0,0,15"/>
                        </StackPanel>
                    </ScrollViewer>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button Content="Cancel" Command="{Binding CancelCommand}" Background="Transparent" BorderBrush="{StaticResource TextSecondaryBrush}" BorderThickness="1" Foreground="{StaticResource TextBrush}" Margin="0,0,10,0"/>
                        <Button Content="Save" Command="{Binding SaveCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
