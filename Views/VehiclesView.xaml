<UserControl x:Class="CarRental.Desktop.Views.VehiclesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:controls="clr-namespace:CarRental.Desktop.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="+ Add Vehicle" Command="{Binding AddVehicleCommand}" Width="150"/>
            <CheckBox IsChecked="{Binding IsGridView}" Content="Card View" Margin="20,0,0,0" VerticalAlignment="Center"/>
            <Button Content="Edit" Command="{Binding EditVehicleCommand}" Margin="20,0,0,0" Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource TextBrush}"/>
            <Button Content="Delete" Command="{Binding DeleteVehicleCommand}" Margin="10,0,0,0" Background="{StaticResource ErrorBrush}" Foreground="White"/>
        </StackPanel>

        <!-- DataGrid -->
        <!-- DataGrid (List View) -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Vehicles}" SelectedItem="{Binding SelectedVehicle}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Style>
                <Style TargetType="DataGrid">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsGridView}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Style>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Make" Binding="{Binding Make}" Width="*"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="*"/>
                <DataGridTextColumn Header="Year" Binding="{Binding Year}" Width="80"/>
                <DataGridTextColumn Header="License Plate" Binding="{Binding LicensePlate}" Width="120"/>
                <DataGridTextColumn Header="Daily Rate" Binding="{Binding DailyRate, StringFormat=C}" Width="100"/>
                <DataGridTemplateColumn Header="Status" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource SurfaceBrush}" CornerRadius="4" Padding="5,2">
                                <TextBlock Text="{Binding Status}" HorizontalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Card View (Grid View) -->
        <ScrollViewer Grid.Row="1">
            <ScrollViewer.Style>
                <Style TargetType="ScrollViewer">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsGridView}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ScrollViewer.Style>
            <ItemsControl ItemsSource="{Binding Vehicles}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:VehicleCard/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Edit Overlay -->
        <Grid Grid.RowSpan="2" Background="#AA000000" Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="{StaticResource BackgroundBrush}" Width="500" Height="550" CornerRadius="10" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="30">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="{Binding EditTitle}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,20"/>

                    <ScrollViewer Grid.Row="1">
                        <StackPanel>
                            <Label Content="Make"/>
                            <TextBox Text="{Binding Make}" Margin="0,0,0,15"/>

                            <Label Content="Model"/>
                            <TextBox Text="{Binding Model}" Margin="0,0,0,15"/>

                            <Label Content="Year"/>
                            <TextBox Text="{Binding Year}" Margin="0,0,0,15"/>

                            <Label Content="License Plate"/>
                            <TextBox Text="{Binding LicensePlate}" Margin="0,0,0,15"/>

                            <Label Content="Daily Rate"/>
                            <TextBox Text="{Binding DailyRate}" Margin="0,0,0,15"/>

                            <Label Content="Status"/>
                            <ComboBox SelectedValue="{Binding Status}" SelectedValuePath="Content" Height="40" Margin="0,0,0,15">
                                <ComboBoxItem Content="Available"/>
                                <ComboBoxItem Content="Rented"/>
                                <ComboBoxItem Content="Maintenance"/>
                            </ComboBox>

                            <Label Content="Image"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                <TextBox Text="{Binding ImagePath}" Width="250" IsReadOnly="True" VerticalContentAlignment="Center"/>
                                <Button Content="Browse..." Command="{Binding BrowseImageCommand}" Width="80" Margin="10,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button Content="Cancel" Command="{Binding CancelCommand}" Background="Transparent" BorderBrush="{StaticResource TextSecondaryBrush}" BorderThickness="1" Foreground="{StaticResource TextBrush}" Margin="0,0,10,0"/>
                        <Button Content="Save" Command="{Binding SaveCommand}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
